#include <iostream>
using namespace std;
int main()
{
    int a[] = {1,2,4,5};
    int length=0;
    for(int i=0;i<sizeof(a)/sizeof(a[0]);i++)
    {
        length++;
    }
    cout<<" The length of the array is "<<length;
}
